import { NeedCorrectionComponent } from './need-correction.component';

describe('NeedCorrectionComponent', () => {
  let component: NeedCorrectionComponent;

  beforeEach(() => {
    component = new NeedCorrectionComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
