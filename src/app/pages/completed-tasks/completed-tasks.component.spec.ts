import { CompletedTasksComponent } from './completed-tasks.component';

describe('CompletedTasksComponent', () => {
  let component: CompletedTasksComponent;

  beforeEach(() => {
    component = new CompletedTasksComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
