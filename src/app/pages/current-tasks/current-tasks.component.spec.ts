import { CurrentTasksComponent } from './current-tasks.component';

describe('CurrentTasksComponent', () => {
  let component: CurrentTasksComponent;

  beforeEach(() => {
    component = new CurrentTasksComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
